if (WIN32)
    set(FDOTOOL_TOOLS_INSTALL_DIR ".")
elseif (APPLE)
    set(FDOTOOL_TOOLS_INSTALL_DIR "${CMAKE_INSTALL_PREFIX}/fdotool")
else ()
    set(FDOTOOL_TOOLS_INSTALL_DIR "${CMAKE_INSTALL_LIBDIR}/fdotool")
endif ()
